{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto mt-16 p-8 bg-gray-900 rounded-lg shadow-md text-white">
        <h1 class="text-3xl font-bold mb-6 text-green-500 text-center">Modifier l'utilisateur</h1>

        <div class="mb-6">
            {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>

        <div class="flex justify-between">
            <a href="{{ path('app_user_index') }}"
               class="bg-gray-700 hover:bg-gray-600 text-white px-4 py-2 rounded transition">
                Retour à la liste
            </a>

            {{ include('user/_delete_form.html.twig', { 'button_class': 'bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded transition' }) }}
        </div>
    </div>
{% endblock %}
